const Loading = () => {
  return (
    <div className='w-scree text-white fixed left-0 top-0 flex w-screen h-screen items-center justify-center'>
      Animals are loading...
    </div>
  );
};

export default Loading;
